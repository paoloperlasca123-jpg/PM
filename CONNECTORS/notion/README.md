# Conector: Notion

**Estado:** no configurado.

## API oficial

Sí — Notion API, mediante un token de integración interna (no OAuth
público, más simple de configurar para un solo workspace).

## Automatizaciones posibles

- Reflejar `manifest.json` de cada proyecto como fila en una base
  de datos de Notion, útil para vista de equipo sin acceso al
  filesystem.
- Sincronizar `etapa_actual` como estado visible.

No se activa hasta que se configure explícitamente.
